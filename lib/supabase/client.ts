/**
 * JAECOO Palembang — Supabase Client
 *
 * STEP 4A: Stub — connect when ready.
 *
 * To activate Supabase:
 * 1. npm install @supabase/supabase-js
 * 2. Add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY to .env.local
 * 3. Uncomment the createClient call below
 * 4. Replace static imports in lib/data/models.ts with queries from lib/supabase/queries.ts
 *
 * The query interface is identical — getModels(), getModelBySlug(), getModelSlugs()
 * — so page components need NO changes when switching from static to Supabase.
 */

// import { createClient } from '@supabase/supabase-js'
//
// const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
// const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
//
// export const supabase = createClient(supabaseUrl, supabaseAnonKey)

export {}; // placeholder — remove when activating
